Block comments
``` javascript
/**
 * @param {string} name
 */
```

Inline comments
``` javascript
//inline comment
```

## hotkeys

`ctrl` + `/` – comment one line
`shift` + `alt` + `a` – comment multiline (vsc specific)
